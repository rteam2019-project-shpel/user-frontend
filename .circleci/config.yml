version: 2
jobs:
  build:
    docker:
      - image: debian:stretch
      - image: circleci/node:7.10

    steps:
      - checkout

      - run:
          name: Greeting
          command: echo Hello, world.

      - run:
          name: Print the Current Time
          command: date
      
      - run:
          name: npm-install
          command: npm install
          
      - run:
          name: npm-test
          command: npm test
